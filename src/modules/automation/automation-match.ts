export enum AutomationMatchMode {
  EQUALS = 'equals',
  CONTAINS = 'contains',
  STARTS_WITH = 'startsWith',
  REGEX = 'regex',
}

export enum AutomationChatContext {
  ALL = 'all',
  GROUP = 'group',
  PRIVATE = 'private',
}
